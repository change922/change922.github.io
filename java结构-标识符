/**
 * 测试标识符，标识符是用来给类，方法，变量，包命名的
 * @author Administrator
 *
 */

public class TestIdentifer {
	public static void main(String[] args) {
		System.out.println("标识符必须是字母(汉字也是字母)._或者$开头，其他部分可以是任意组合，可以是数字"
				+ "标识符要区分大小写，长度不限，标识符不能是关键字"
				+ "类名的第一个单词的首字母大小，后面单词的首字母都大写，其他小写"
				+ "方法和变量的名字大小写遵循驼峰原则，第一个单词小写，后面的单词每个首字母大写"
				+ "汉字也是字母，但一般不用");
	
	}

}

